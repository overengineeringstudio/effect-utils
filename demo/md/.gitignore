# Runtime working copies — regenerated by reset.sh from ./seed, never committed.
# The committed clean state lives in ./seed; the live page ids/urls are per-run.
stage/*.nmd
stage/*.conflict.roughdraft.md
stage/.notion-md/
.demo-state/

# E2E harness evidence — screenshots, timeline, report (per-run, never committed).
evidence/
stage/watch.log
