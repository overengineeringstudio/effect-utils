/**
 * Storybook preview configuration for @overeng/tui-react
 *
 * Uses the shared TUI Storybook utilities (dogfooding our own export).
 */

import { tuiPreview } from '../src/storybook/mod.ts'

export default tuiPreview
