// Mach-O admission fixture: a dependency-free TypeScript CLI whose compiled
// product must pass the strict mach-o-dynamic/v1 import contract on
// aarch64-darwin (descriptor emission, Nix admission, sterile execution).
console.log("macho-hello ok")
