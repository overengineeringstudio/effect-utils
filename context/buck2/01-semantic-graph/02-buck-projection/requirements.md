# Buck Projection Requirements

**Role:** This subsystem defines how the parent
[semantic graph](../requirements.md) becomes reviewable Buck inputs. Genie is
the current projection mechanism; it does not own semantic package facts,
dependency closures, Buck rule behavior, or build-tool selection.

## Context

The Buck projector emits committed configuration whose freshness is checked
locally and in CI. Generation makes outputs read-only as a local editing
guardrail; Git checkout modes are not freshness identity because Git does not
preserve ordinary write bits. The normalized semantic graph is its complete
semantic input.

## Assumptions

- **BUCK.GRAPH.PROJ-A01 Parent graph:** Inputs satisfy the strict,
  runtime-neutral semantic graph contract.
- **BUCK.GRAPH.PROJ-A02 Genie lifecycle:** Discovery, read-only output modes,
  and repository freshness reporting satisfy Genie's root VRS.

## Acceptable Tradeoffs

- **BUCK.GRAPH.PROJ-T01 Generated configuration:** Package-local Buck
  configuration may be committed generated output when this makes review and
  cold Buck analysis deterministic.

## Requirements

### Must remain a pure projection

- **BUCK.GRAPH.PROJ-R01 Data-only input:** Projection must consume normalized
  semantic graph values and declared renderer configuration only. It must not
  reconstruct semantic facts from generated package, project, or test files.
- **BUCK.GRAPH.PROJ-R02 No build execution:** Projection must not invoke a
  compiler, bundler, type checker, test runner, package builder, Buck action, or
  compiler dependency-analysis mode.
- **BUCK.GRAPH.PROJ-R03 No tool identity semantics:** Concrete Bun, `tsgo`,
  Rust, Cargo, Nix store, or platform identities must not affect semantic shard
  bytes.

### Must emit stable local projections

- **BUCK.GRAPH.PROJ-R04 Package-local shards:** Each package must receive a
  package-local generated shard derived from that package's normalized model
  and stable shared schema only.
- **BUCK.GRAPH.PROJ-R05 Deterministic bytes:** Equal normalized input must emit
  byte-identical output regardless of authored set ordering or equivalent
  renderer implementation.
- **BUCK.GRAPH.PROJ-R06 Unrelated-shard stability:** A change confined to one
  package must leave every unrelated package shard byte-identical.
- **BUCK.GRAPH.PROJ-R07 Thin semantic surface:** Generated shards must express
  semantic target declarations through a stable Buck facade. They must not
  inline action commands, toolchain paths, or duplicated rule implementations.

### Must make freshness and provenance truthful

- **BUCK.GRAPH.PROJ-R08 Authoritative freshness:** One supported Genie check
  must regenerate and compare all semantic graph projections and fail on
  missing, stale, or conflicting outputs. It must not reject a byte-identical
  fresh checkout solely because Git restored an ordinary file as writable.
- **BUCK.GRAPH.PROJ-R09 Non-semantic provenance:** Operational provenance may
  identify an authored source, schema version, and regeneration command, but a
  generator or tool implementation change that preserves output semantics must
  not churn semantic shard bytes.
- **BUCK.GRAPH.PROJ-R10 Completeness separation:** Source-file completeness
  validation must enforce the parent graph contract without compiling sources
  or forcing matched file enumeration into generated bytes. Genie validates and
  renders typed file-set declarations; Buck observes matching membership and
  the independent census validates total, non-ambiguous ownership.
- **BUCK.GRAPH.PROJ-R11 Causal projection tests:** Tests must prove stable
  ordering, equivalent-renderer bytes, unrelated-shard stability, package move
  behavior, target split locality, and refusal of compile/process capabilities.
