# Spec - Datasource Control Plane

This document specifies the datasource control-plane role inside the datasource
Markdown workspace realization. It builds on [requirements.md](./requirements.md).

The datasource control plane is realized by
`@overeng/notion-datasource-sync`.

Package-local details remain in
`packages/@overeng/notion-datasource-sync/docs/vrs/`.
