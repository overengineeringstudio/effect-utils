# Generated file - DO NOT EDIT
# Source: storybook-preview-deploy.yml.genie.ts

name: Storybook Preview Deploy

permissions: {}

on:
  workflow_run:
    workflows: [Storybook Preview Build]
    types: [completed]

concurrency:
  group: '${{ github.workflow }}-${{ github.event.workflow_run.head_repository.full_name }}-${{ github.event.workflow_run.head_branch }}'
  cancel-in-progress: true

jobs:
  resolve-preview:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request' }}
    runs-on:
      [namespace-profile-linux-x86-64, 'namespace-features:github.run-id=${{ github.run_id }}']
    timeout-minutes: 5
    permissions:
      pull-requests: read
    defaults:
      run:
        shell: bash
    outputs:
      deploy: ${{ steps.pull-request.outputs.deploy }}
      number: ${{ steps.pull-request.outputs.number }}
      head-sha: ${{ steps.pull-request.outputs.head-sha }}
      head-repo: ${{ steps.pull-request.outputs.head-repo }}
    steps:
      - id: pull-request
        name: Resolve pull request from the triggering run
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_EVENT: ${{ github.event.workflow_run.event }}
          RUN_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
          RUN_HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          RUN_HEAD_BRANCH: ${{ github.event.workflow_run.head_branch }}
          RUN_HEAD_REPO: ${{ github.event.workflow_run.head_repository.full_name }}
          RUN_PR_NUMBER: ${{ github.event.workflow_run.pull_requests[0].number }}
        run: |
          set -euo pipefail
          test "$RUN_EVENT" = pull_request
          test "$RUN_CONCLUSION" = success
          [[ "$RUN_HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]
          test -n "$RUN_HEAD_REPO"
          test -n "$RUN_HEAD_BRANCH"
          pr_number="$RUN_PR_NUMBER"
          if [ -z "$pr_number" ]; then
            # `workflow_run.pull_requests` is empty for fork heads; match the open PR by head.
            pr_number=$(gh api --paginate "/repos/$GITHUB_REPOSITORY/pulls?state=open&per_page=100" --slurp \
              | jq -r --arg repo "$RUN_HEAD_REPO" --arg ref "$RUN_HEAD_BRANCH" --arg sha "$RUN_HEAD_SHA" \
                '[.[][] | select(.head.repo.full_name == $repo and .head.ref == $ref and .head.sha == $sha)] | if length == 1 then .[0].number else "" end')
          fi
          deploy=true
          if [ -z "$pr_number" ]; then
            echo "::notice::No open pull request matches $RUN_HEAD_REPO@$RUN_HEAD_SHA; skipping preview deploy"
            deploy=false
          else
            [[ "$pr_number" =~ ^[1-9][0-9]*$ ]]
            pr_json=$(gh api "/repos/$GITHUB_REPOSITORY/pulls/$pr_number")
            test "$(jq -r '.head.repo.full_name' <<<"$pr_json")" = "$RUN_HEAD_REPO"
            if [ "$(jq -r '.state' <<<"$pr_json")" != open ] || [ "$(jq -r '.head.sha' <<<"$pr_json")" != "$RUN_HEAD_SHA" ]; then
              echo "::notice::PR #$pr_number is closed or its head moved past $RUN_HEAD_SHA; skipping stale preview deploy"
              deploy=false
            fi
          fi
          if [ "$RUN_HEAD_REPO" != "$GITHUB_REPOSITORY" ]; then
            echo "::notice::Fork pull request from $RUN_HEAD_REPO; fork previews are not deployed"
            deploy=false
          fi
          {
            echo "deploy=$deploy"
            echo "number=$pr_number"
            echo "head-sha=$RUN_HEAD_SHA"
            echo "head-repo=$RUN_HEAD_REPO"
          } >> "$GITHUB_OUTPUT"
    env:
      FORCE_SETUP: '1'
      CI: 'true'
      BUCK2_NO_REMOTE_CACHE: '1'
  deploy-preview:
    needs: [resolve-preview]
    if: ${{ needs.resolve-preview.outputs.deploy == 'true' }}
    runs-on:
      [namespace-profile-linux-x86-64, 'namespace-features:github.run-id=${{ github.run_id }}']
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      pull-requests: read
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout default-branch tooling
        uses: actions/checkout@v6
        with:
          ref: ${{ github.workflow_sha }}
          persist-credentials: false
      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          extra-conf: |
            experimental-features = nix-command flakes
            accept-flake-config = true
            extra-substituters = https://overeng-effect-utils.cachix.org
            extra-trusted-public-keys = overeng-effect-utils.cachix.org-1:KFmqYNF6Q7ZzVYPl2znpJYZGEolage9YNCA9res6vKc=
            access-tokens = github.com=${{ github.token }}
          summarize: true
      - name: Provide cachix CLI from nixpkgs
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          out=$(nix build --no-link --print-out-paths nixpkgs#cachix)
          echo "$out/bin" >> "$GITHUB_PATH"
      - name: Enable Cachix cache
        uses: cachix/cachix-action@v17
        with:
          name: overeng-effect-utils
          skipPush: true
      - name: Prepare CI helper scripts
        shell: bash
        run: |
          cd "${GITHUB_WORKSPACE:-$PWD}" && set -euo pipefail
          scripts_src='genie/ci-scripts'
          scripts_dst='${{ runner.temp }}/composition-state/ci-runtime'
          if [ ! -d "$scripts_src" ]; then
            echo "::error::CI helper script directory is missing: $scripts_src"
            exit 1
          fi
          rm -rf "$scripts_dst"
          mkdir -p "$scripts_dst"
          cp -R "$scripts_src/." "$scripts_dst/"
          rm -f "$scripts_dst"/*.genie.ts
          chmod +x "$scripts_dst"/*.sh
      - name: Use pinned devenv from lock
        run: |
          cd "${GITHUB_WORKSPACE:-$PWD}" && DEVENV_REV=$(jq -r .nodes.devenv.locked.rev 'devenv.lock')
          if [ -z "$DEVENV_REV" ] || [ "$DEVENV_REV" = "null" ]; then
            printf '::error::%s missing .nodes.devenv.locked.rev\n' 'devenv.lock'
            exit 1
          fi
          echo "DEVENV_REV=$DEVENV_REV" >> "$GITHUB_ENV"
          echo "Pinned devenv rev: $DEVENV_REV"
        shell: bash
      - name: Resolve devenv
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'resolve devenv (devenv.lock)' 'cd "${GITHUB_WORKSPACE:-$PWD}" && '"'"'${{ runner.temp }}/composition-state/ci-runtime/resolve-devenv.sh'"'"' '"'"'devenv.lock'"'"''
        shell: bash
      - name: Download staged Netlify output
        uses: actions/download-artifact@v4
        with:
          name: netlify-preview-static
          path: '${{ runner.temp }}/netlify-preview-stage'
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
      - id: deploy
        name: Deploy staged storybook previews to Netlify
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          NETLIFY_STAGE_DIR: '${{ runner.temp }}/netlify-preview-stage'
          NETLIFY_PREVIEW_PR: ${{ needs.resolve-preview.outputs.number }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        run: |
          workflow_report_dir="${RUNNER_TEMP:-/tmp}/workflow-reports"
          mkdir -p "$workflow_report_dir"
          workflow_report_path="$(mktemp "$workflow_report_dir/netlify-storybooks.XXXXXX.jsonl")"
          export WORKFLOW_REPORT_OUTPUT_FILE="$workflow_report_path"
          deploy_ran=1
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'devenv tasks run netlify:deploy-staged --show-output --input type=pr --input "pr=$NETLIFY_PREVIEW_PR" --input "stageDir=$NETLIFY_STAGE_DIR" --input missingAuthPolicy=skip --input urlEnvKey=NETLIFY_DEPLOY_URL_STORYBOOK' 'cd "${GITHUB_WORKSPACE:-$PWD}" && if [ -n "${NIX_CONFIG:-}" ]; then NIX_CONFIG_WITH_APPEND=$(printf '"'"'%s\n%s'"'"' "$NIX_CONFIG" '"'"'restrict-eval = false'"'"'); else NIX_CONFIG_WITH_APPEND='"'"'restrict-eval = false'"'"'; fi; NIX_CONFIG="$NIX_CONFIG_WITH_APPEND" DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" tasks run netlify:deploy-staged --show-output --input type=pr --input "pr=$NETLIFY_PREVIEW_PR" --input "stageDir=$NETLIFY_STAGE_DIR" --input missingAuthPolicy=skip --input urlEnvKey=NETLIFY_DEPLOY_URL_STORYBOOK'
          if [ "$deploy_ran" = "1" ] && [ ! -s "$workflow_report_path" ]; then
            echo "Error: ci-tools did not emit a Netlify workflow report record." >&2
            exit 1
          fi
          if [ "$deploy_ran" = "1" ]; then
            echo "workflow_report_path=$workflow_report_path" >> "$GITHUB_OUTPUT"
          fi
      - name: Collect workflow report bundle
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GH_TOKEN: ${{ github.token }}
          WORKFLOW_REPORT_BUNDLE_ID: storybook-preview
          WORKFLOW_REPORT_INPUT_PATHS_JSON: '["${{ steps.deploy.outputs.workflow_report_path }}"]'
          WORKFLOW_REPORT_OUTPUT_PATH: '${{ runner.temp }}/workflow-reports/netlify-preview/bundle.json'
          WORKFLOW_REPORT_RECORD_MARKER: 'WORKFLOW_REPORT_V1: '
          WORKFLOW_REPORT_ALLOW_MISSING_INPUT: '1'
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'devenv tasks run workflow-report:collect-bundle --show-output' 'cd "${GITHUB_WORKSPACE:-$PWD}" && if [ -n "${NIX_CONFIG:-}" ]; then NIX_CONFIG_WITH_APPEND=$(printf '"'"'%s\n%s'"'"' "$NIX_CONFIG" '"'"'restrict-eval = false'"'"'); else NIX_CONFIG_WITH_APPEND='"'"'restrict-eval = false'"'"'; fi; NIX_CONFIG="$NIX_CONFIG_WITH_APPEND" DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" tasks run workflow-report:collect-bundle --show-output'
      - name: Render workflow report comment
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          WORKFLOW_REPORT_EVENT_NAME: pull_request
          WORKFLOW_REPORT_PR_NUMBER: ${{ needs.resolve-preview.outputs.number }}
          WORKFLOW_REPORT_BUNDLE_PATH: '${{ runner.temp }}/workflow-reports/netlify-preview/bundle.json'
          WORKFLOW_REPORT_COMMENT_BODY_PATH: '${{ runner.temp }}/workflow-reports/netlify-preview/comment.md'
          WORKFLOW_REPORT_SUMMARY_PATH: '${{ runner.temp }}/workflow-reports/netlify-preview/summary.md'
          WORKFLOW_REPORT_TITLE: Storybook Previews
          WORKFLOW_REPORT_NO_RECORDS_MESSAGE: No storybooks were deployed.
          WORKFLOW_REPORT_STATE_ID: storybook-preview
          WORKFLOW_REPORT_ENTRY_ID: ${{ needs.resolve-preview.outputs.head-sha }}
          WORKFLOW_REPORT_ENTRY_LABEL: ${{ format('PR {0}', needs.resolve-preview.outputs.number) }}
          WORKFLOW_REPORT_CREATED_AT_UTC: ''
          WORKFLOW_REPORT_TIME_ZONE: UTC
          WORKFLOW_REPORT_MANAGED_MARKER: '<!-- workflow-report:managed -->'
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'devenv tasks run workflow-report:render-comment-body --show-output' 'cd "${GITHUB_WORKSPACE:-$PWD}" && if [ -n "${NIX_CONFIG:-}" ]; then NIX_CONFIG_WITH_APPEND=$(printf '"'"'%s\n%s'"'"' "$NIX_CONFIG" '"'"'restrict-eval = false'"'"'); else NIX_CONFIG_WITH_APPEND='"'"'restrict-eval = false'"'"'; fi; NIX_CONFIG="$NIX_CONFIG_WITH_APPEND" DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" tasks run workflow-report:render-comment-body --show-output'
      - name: Upload rendered preview comment
        uses: actions/upload-artifact@v4
        with:
          name: netlify-preview-comment
          path: '${{ runner.temp }}/workflow-reports/netlify-preview'
          if-no-files-found: error
          retention-days: 1
    env:
      FORCE_SETUP: '1'
      CI: 'true'
      BUCK2_NO_REMOTE_CACHE: '1'
  publish-preview-comment:
    needs: [resolve-preview, deploy-preview]
    runs-on:
      [namespace-profile-linux-x86-64, 'namespace-features:github.run-id=${{ github.run_id }}']
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout default-branch tooling
        uses: actions/checkout@v6
        with:
          ref: ${{ github.workflow_sha }}
          persist-credentials: false
      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          extra-conf: |
            experimental-features = nix-command flakes
            accept-flake-config = true
            extra-substituters = https://overeng-effect-utils.cachix.org
            extra-trusted-public-keys = overeng-effect-utils.cachix.org-1:KFmqYNF6Q7ZzVYPl2znpJYZGEolage9YNCA9res6vKc=
            access-tokens = github.com=${{ github.token }}
          summarize: true
      - name: Provide cachix CLI from nixpkgs
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          out=$(nix build --no-link --print-out-paths nixpkgs#cachix)
          echo "$out/bin" >> "$GITHUB_PATH"
      - name: Enable Cachix cache
        uses: cachix/cachix-action@v17
        with:
          name: overeng-effect-utils
          skipPush: true
      - name: Prepare CI helper scripts
        shell: bash
        run: |
          cd "${GITHUB_WORKSPACE:-$PWD}" && set -euo pipefail
          scripts_src='genie/ci-scripts'
          scripts_dst='${{ runner.temp }}/composition-state/ci-runtime'
          if [ ! -d "$scripts_src" ]; then
            echo "::error::CI helper script directory is missing: $scripts_src"
            exit 1
          fi
          rm -rf "$scripts_dst"
          mkdir -p "$scripts_dst"
          cp -R "$scripts_src/." "$scripts_dst/"
          rm -f "$scripts_dst"/*.genie.ts
          chmod +x "$scripts_dst"/*.sh
      - name: Use pinned devenv from lock
        run: |
          cd "${GITHUB_WORKSPACE:-$PWD}" && DEVENV_REV=$(jq -r .nodes.devenv.locked.rev 'devenv.lock')
          if [ -z "$DEVENV_REV" ] || [ "$DEVENV_REV" = "null" ]; then
            printf '::error::%s missing .nodes.devenv.locked.rev\n' 'devenv.lock'
            exit 1
          fi
          echo "DEVENV_REV=$DEVENV_REV" >> "$GITHUB_ENV"
          echo "Pinned devenv rev: $DEVENV_REV"
        shell: bash
      - name: Resolve devenv
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'resolve devenv (devenv.lock)' 'cd "${GITHUB_WORKSPACE:-$PWD}" && '"'"'${{ runner.temp }}/composition-state/ci-runtime/resolve-devenv.sh'"'"' '"'"'devenv.lock'"'"''
        shell: bash
      - name: Download rendered preview comment
        uses: actions/download-artifact@v4
        with:
          name: netlify-preview-comment
          path: '${{ runner.temp }}/workflow-reports/netlify-preview'
      - name: Publish workflow report
        if: always() && !cancelled()
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          WORKFLOW_REPORT_EVENT_NAME: pull_request
          WORKFLOW_REPORT_PR_NUMBER: ${{ needs.resolve-preview.outputs.number }}
          WORKFLOW_REPORT_HEAD_REPO: ${{ needs.resolve-preview.outputs.head-repo }}
          WORKFLOW_REPORT_STATE_ID: storybook-preview
          WORKFLOW_REPORT_COMMENT_BODY_PATH: '${{ runner.temp }}/workflow-reports/netlify-preview/comment.md'
          WORKFLOW_REPORT_SUMMARY_PATH: '${{ runner.temp }}/workflow-reports/netlify-preview/summary.md'
          WORKFLOW_REPORT_MANAGED_MARKER: '<!-- workflow-report:managed -->'
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'devenv tasks run workflow-report:publish --show-output' 'cd "${GITHUB_WORKSPACE:-$PWD}" && if [ -n "${NIX_CONFIG:-}" ]; then NIX_CONFIG_WITH_APPEND=$(printf '"'"'%s\n%s'"'"' "$NIX_CONFIG" '"'"'restrict-eval = false'"'"'); else NIX_CONFIG_WITH_APPEND='"'"'restrict-eval = false'"'"'; fi; NIX_CONFIG="$NIX_CONFIG_WITH_APPEND" DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" tasks run workflow-report:publish --show-output'
    env:
      FORCE_SETUP: '1'
      CI: 'true'
      BUCK2_NO_REMOTE_CACHE: '1'
